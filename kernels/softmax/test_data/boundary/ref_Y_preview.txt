M=1 N=1
Reference (torch.nn.functional.softmax(X, dim=1)):
Y[row=0, 0:1] = 1.000000

Run on target machine:
  ./softmax 1 1 X.bin Y_out.bin
Compare its stdout line (same "Y[row=0, 0:N] = ..." format) against the
reference line above. atol=1e-5 per project validation standard
(kernel does the standard row-max-subtracted exp/sum in float32,
same numerical approach as this reference).
